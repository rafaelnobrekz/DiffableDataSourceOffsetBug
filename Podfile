# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DiffableDataSourceOffsetBug' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'DiffableDataSources'
#  pod 'DiffableDataSources', :git => 'https://github.com/rafaelnobrekz/DiffableDataSources.git', :branch => 'native-when-available'
end
